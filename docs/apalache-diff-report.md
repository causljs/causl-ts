# Apalache Differential Report

_Generated: 1777869350Z (Unix epoch seconds)_

_Auto-generated by `cargo run -p apalache-diff`. Do not hand-edit; re-run the binary to refresh._

> **Current state (as of v0.9.0).** The sample table below was captured on a workstation where `apalache-mc` was not on `PATH`; every Apalache cell reads `binary-unavailable` and every `combined_status` collapses to `unknown`. The Tier-3 nightly CI job (which provisions Apalache) populates the table fully; the binary's verdict-parser surface is exercised by `tools/enumerator/diff/tests/run_apalache.rs`. A `binary-unavailable` row is the harness's graceful-degradation arm, not a failure. The 4-way classifier section that PR #1101 (#1070) added is rendered below the 2-way table — it does NOT consult Apalache (see prose footer).

## Per-scenario verdicts

| scenario | tla file | invariant | apalache verdict | enumerator verdict | verdict_status | combined_status |
| --- | --- | --- | --- | --- | --- | --- |
| single_input_commit | `tools/enumerator/corpus/apalache/single_input_commit.tla` | `Monotonic` | binary-unavailable | held | unknown | unknown |
| cycle_two_deriveds | `tools/enumerator/corpus/apalache/cycle_two_deriveds.tla` | `Acyclic` | binary-unavailable | violated (reachable-cycle) | unknown | unknown |
| glitch_propagation_minimal | `tools/enumerator/corpus/apalache/glitch_propagation_minimal.tla` | `GlitchFree` | binary-unavailable | held | unknown | unknown |
| dispose_then_read | `tools/enumerator/corpus/apalache/dispose_then_read.tla` | `UseAfterDisposeFree` | binary-unavailable | held | unknown | unknown |
| cross_graph_read_uncovered | `tools/enumerator/corpus/apalache/cross_graph_read_uncovered.tla` | `AllCrossGraphReadsBridged` | binary-unavailable | held | unknown | unknown |
| subscribe_callback_cascade | `tools/enumerator/corpus/apalache/subscribe_callback_cascade.tla` | `NoCascadingCommit` | binary-unavailable | held | unknown | unknown |
| monotonic_commit_violation | `tools/enumerator/corpus/apalache/monotonic_commit_violation.tla` | `Monotonic` | binary-unavailable | held | unknown | unknown |
| replay_divergence | `tools/enumerator/corpus/apalache/replay_divergence.tla` | `Deterministic` | binary-unavailable | held | unknown | unknown |
| legacy_allow_bridge | `tools/enumerator/corpus/apalache/legacy_allow_bridge.tla` | `LegacyAllowSuppresses` | binary-unavailable | held | unknown | unknown |
| process_exit_scope_subscribe | `tools/enumerator/corpus/apalache/process_exit_scope_subscribe.tla` | `ProcessExitScopeSuppresses` | binary-unavailable | held | unknown | unknown |

## Agreement summary

Held: 0, Violated: 0, BinaryUnavailable: 10, Skipped: 0 (total: 10)

## Verdict status summary

match: 0, divergence: 0, unknown: 10 (total: 10)

## Combined verdict summary

triple_agree: 0, apalache_only_agrees: 0, enumerator_only_agrees: 0, none_agree: 0, unknown: 10 (total: 10)

## 4-way classifier

PR #1101 (closes #1070) shipped a four-way differential that compares (a) the TS engine reading `commitInternal` out of `packages/core/src/graph.ts`, (b) the `tools/engine-rs-bridge-serde` WASM bridge, (c) the `tools/engine-rs-bridge-gc` WASM bridge with `js-string-builtins`, and (d) the Rust enumerator's bounded BFS at `Bound::tier1()`. The classifier names the disagreeing subset — `Unanimous`, `TsOnlyDiffers`, `WasmDiffersFromCanonical`, `RustDiffersFromJs`, `UnanimousShapeDivergentHash`, `SplitDecision`, or `Unknown` — per `tools/enumerator/diff/src/four_way.rs`. The four-way table renders below the two-way table when the runner is invoked with `--four-way` (rendering harness: `four_way::append_four_way_section`). The classifier intentionally does NOT consult Apalache: Apalache's verdict is already the `expected_apalache` truth source the 2-way classifier consumes, so a third quotation would be self-referential.

On the EPIC-7 corpus (10 scenarios) plus the five canonical seeds in `CANONICAL_SEED_IDS`, the live four-way run produces 14 `unanimous` rows and 1 categorised exception (`cycle_two_deriveds`, classified as `four-way-architectural` because the JS-driven legs read the cycle structurally `held` from `parseCauslModel` while the Rust enumerator's bounded BFS hits the `ReachableCycle` race-class). Both exception rows in `tools/apalache-diff/mapping.toml` (`glitch_propagation_minimal` and `cycle_two_deriveds`) are scheduled for re-validation under the #1141 panel-review track once the Rust-engine port work (epic #1133) lands — the exceptions encode architectural asymmetries the current TS-canonical encoding makes load-bearing, and a Rust-canonical reading may collapse one or both rows back into the unanimous arm.

## Exceptions

- **glitch_propagation_minimal** (encoding-asymmetric) — The TLA+ side encodes §3 Theorem 2's value-equality invariant directly (sum = a + b at every state) and the Commit action preserves it by construction — Apalache verdicts `held`. The Rust side encodes the structural-IR variant: the linter's GlitchPropagation pass detects an inconsistent `changedNodes` set (sum changed, but neither dep changed), which has no faithful TLA+ encoding because TLA+ does not model the `changedNodes` artifact. The two oracles witness the same race-class from complementary angles. The differential runner must accept this specific (held, violated) pair on this specific scenario. ([iasbuilt/causl#574](https://github.com/iasbuilt/causl/issues/574); flagged for re-validation under #1141.)
- **cycle_two_deriveds** (four-way-architectural) — The three JS-driven legs (TS engine + serde-WASM bridge + gc-WASM bridge) all classify the row's `CauslModel` as structurally `held` because `parseCauslModel` does not evaluate the cycle that lives in the derived bodies. The Rust enumerator runs a bounded BFS that hits a `ReachableCycle` race-class verdict at `Bound::tier1()`. The strict four-way gate (`--strict --four-way`) excuses the row via this exception until the Rust port lands and the JS canonical encoding shifts. (#1141 panel-review track.)

---

_Verdict columns: `apalache verdict` is the apalache-mc outcome; `enumerator verdict` is the bounded BFS verdict at `Bound::tier1()`; `combined_status` is the three-way agreement classifier per SPEC §16.7 (#574). The 4-way classifier (#1070, shipped via #1101) consumes only the TS / WASM-serde / WASM-gc-builtins / Rust-enumerator legs; Apalache is consulted exclusively by the 2-way harness above._
